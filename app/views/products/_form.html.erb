<%= form_with(model: products, local: true) do |form| %>
  <% if products.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(products.errors.count, "error") %> prohibited this products from being saved:</h2>

      <ul>
        <% products.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :description %>
    <%= form.text_field :description %>
  </div>

  <div class="field">
    <%= form.label :image %>
    <%= form.text_field :image %>
  </div>

  <div class="field">
    <%= form.label :price %>
    <%= form.number_field :price %>
  </div>

  <div class="field">
    <%= form.label :brand %>
    <%= form.text_field :brand %>
  </div>

  <div class="field">
    <%= form.label :categories_id %>
    <%= form.collection_select(:categories_id, Categories.all, :id, :name ) %>




  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
