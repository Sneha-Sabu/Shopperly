<%= form_with(model: categories, local: true) do |form| %>
  <% if categories.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(categories.errors.count, "error") %> prohibited this categories from being saved:</h2>

      <ul>
        <% categories.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
